services:
  iris:
    build: 
      context: .
    restart: always
    container_name: web-app
    hostname: web-app
    ports:
      - 7771:1972
      - 7772:52773
      - 7773:53773
    command: --check-caps false
    volumes:
      - ./front-end:/front-end